/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["fakestoreapi.com", "via.placeholder.com", "i.dummyjson.com","dummyimage.com"],
  },
};

module.exports = nextConfig;
